Import('*')

env.PartVersion("0.0.0")
env.PartName("part2")
env.DependsOn([
               Component('part1',requires=REQ.DEFAULT(internal=True))
               ])

env.VerboseMessage(['gtest'],env['CPPPATH'],env['CPPPATH']==["${PARTIDEXPORTS('part1','*','CPPPATH',0)}"])

